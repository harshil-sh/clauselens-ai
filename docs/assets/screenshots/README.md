# Screenshot Placeholders

Store final README screenshots in this directory using these filenames:

- `upload-page.png`
- `analysis-summary.png`
- `analysis-risks.png`
- `recent-analyses.png`

The root `README.md` already links to these paths as placeholders so images can be dropped in without changing the document structure.
